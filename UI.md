# BrickAgain 最小兩頁式介面

> **這一份描述的是「最小兩頁式」介面，它沒有任何改動。**
> 其後另行授權的**完整介面**是另一支程式（`scripts/35_full_ui.py`，四頁、
> 三個入口、照片辨識與人工修正、配色與組裝步驟），文件在
> [VISION.md](VISION.md) 的「完整介面」一節。兩者並存：完整介面**繼承**這裡的
> handler，所以底下每一條傳輸層拒絕都是**同一段程式**，不是它的複本。
> 本檔的 195 項測試仍然是原本的意思。

```text
第一頁：文字需求＋手動庫存＋方法
   ↓
第二頁：方法與 provenance → 候選證據 → 靜態檢查 → 庫存 → 3D 預覽 → LDraw
```

> **狀態：本次公開版本已完成獨立技術審查。**
> 這份介面是既有交付路徑的組合層，
> **不是新的模型、新的研究結果，也不是任何成效證據。**

**先讀這段。** 這個介面什麼都沒有量測。它印出來的每一個數字都不是指標，
不能和已封存的 Phase 2 評估比較，也不是任何元件有幫助的證據。
它存在的理由是讓既有的交付流程「可以被人操作、被人看見」，不是為了替誰背書。

---

## 一條命令

```bash
./.venv/bin/python scripts/29_ui.py
```

開啟 <http://127.0.0.1:8765/>。就這樣——不需要建置步驟、不需要 Node、
不需要安裝任何東西。

| 選項 | 預設 | 說明 |
|---|---|---|
| `--port` | `8765` | 監聽的 loopback 連接埠；`0` 由系統挑一個 |
| `--host` | `127.0.0.1` | 只接受 loopback；其他位址**具名拒絕**，不是照樣服務 |
| `--catalog` | 與 `scripts/27_delivery.py` 相同 | train-only 目錄檔 |

退出碼：`0` 正常停止（Ctrl-C）；`2` 被拒絕（錯誤選項，或非 loopback 位址）。

---

## 為什麼不是 Streamlit 或 Gradio

規劃文件（§9.16）建議 **Streamlit** 或 **Gradio**，兩者都**沒有安裝在本專案的
`.venv`**，而本輪不得連網安裝套件。依指示先確認能否用現有依賴完成——可以，
因此沒有新增任何依賴，也沒有停下來要求安裝：

- **HTTP 伺服器**：Python 3.13 標準函式庫的 `http.server`。
- **樣板**：**Jinja2 3.1.6**，已安裝且已釘在 `requirements.txt`。
- **3D 預覽**：既有的 `src/rendering/preview.py`（Matplotlib Agg，CPU）。

`requirements.txt` 沒有改動一個字。副作用是這個介面沒有前端框架、沒有建置步驟、
沒有 CDN，也沒有任何外部資產——這正好符合「離線」這個硬性條件。
樣板字串放在 `src/ui/render.py` 而不是獨立的 `.html` 檔，因為公開 snapshot 的
白名單收的是 `src/**/*.py`；把樣板放在 Python 之外會讓公開版靜默壞掉。

---

## 兩頁在做什麼

### 第一頁：需求與庫存

| 欄位 | 說明 |
|---|---|
| 文字需求 | 中文或英文皆可，最長 2000 字 |
| 方法 | 只有兩種：**既有作品比對**、**最低 F-pipeline** |
| Top-N | 1–50，要考慮幾件 train 候選 |
| 八種零件數量 | `1x1 1x2 1x4 1x6 1x8 2x2 2x4 2x6`，留白或 0 表示沒有 |
| 庫存字串（進階） | 例如 `2x4:10,1x2:8`，與八格擇一 |
| CP-SAT time limit／seed | **只在 F-pipeline 顯示** |

庫存不論從哪一種欄位進來，都會組成一條字串交給既有的
`src.demo.showcase.parse_inventory`，**旋轉正規化與所有庫存拒絕規則都沿用它**：
`4x1` 與 `1x4` 是同一項庫存；兩種拼法同時給出會被拒絕，不會被相加，
因為該用哪一個數字不是解析器該猜的。八格與庫存字串同時填寫也一樣拒絕。

**不適用的欄位不會被靜默忽略。** 選了「既有作品比對」還填 time limit 或 seed，
送出會得到一句指名該欄位的拒絕。頁面上的 JavaScript 只是漸進增強
（選到 F-pipeline 才啟用那組欄位）；**伺服器才是權威**，關掉 JavaScript
的結果是拒絕，不是靜默接受。

檢索方式在頁面上就寫明：目前是**確定性詞彙檢索**（Unicode 詞彙 baseline），
**不是多語 embedding**，也不是正式語意檢索成效證據。

**每一個數值入口都是半形、有長度上限、有範圍上限的**，違反任何一項都是具名的
400，不是 500：

| 規則 | 為什麼 |
|---|---|
| 只接受半形 `0-9` | `str.isdigit()` 對阿拉伯-印度數字、天城體等十幾種文字都為真，`int()` 也照收；那會讓同一個庫存有兩種寫法，而報告只會用其中一種印回來 |
| 位數上限 9 | 超過約 4,300 位時 `int()` 會丟出 ValueError，那不是本介面的拒絕型別，會變成 500。用長度擋下來就是受控的 400 |
| 每種零件上限 8000（＝ 20×20×20） | 世界只有這麼多格，任何結構都用不到更多；再大的數字只會讓表單看起來「懂了」其實沒有 |
| time limit 只接受 `2` 或 `2.5` 這種寫法 | 連 `nan`、`inf`、`1e3` 都不會走到 `float()` |

庫存字串裡的數字同樣受這四條規則約束。零件詞彙、旋轉正規化與「兩種拼法不相加」
仍然由 `parse_inventory` 決定——這裡只檢查數字的**形狀**，不接手它的判斷。

### 第二頁：結果與交付

由上而下：

1. **方法與資料來源**——用了哪個方法、檢索方式與其限制、形狀來源、
   `模型是否載入`（永遠是 `False`）、Phase 3C 狀態、執行狀態、
   目錄檔名與**當次目錄 SHA-256**、**凍結 split manifest 的 SHA-256**、
   送出的需求與庫存。
2. **候選**
   - 比對模式：匿名 `catalog_id`、詞彙分數、磚數、所需庫存、缺件、
     完成比例、是否 inventory-buildable。沒有可組候選時明講，
     不會把「最相似」寫成「可組」。
   - F-pipeline：每個候選各自的 `OPTIMAL`／`FEASIBLE`／`INFEASIBLE`／`UNKNOWN`、
     求解秒數、candidate placement 數，以及回傳鋪排、exact cover、庫存、碰撞、
     邊界、接地、連通、可交付逐項結果與失敗理由。
     **逾時與無解是兩種狀態，不會被寫成同一種。**
3. **選中結果的靜態檢查**——parse、零件種類、庫存、邊界、碰撞、連通性、
   `touches_ground`、LDraw 等。全部由 `src.eval.scoring.score_generation`
   計算後直接引用。
4. **庫存使用量與剩餘**——負數如實印成負數並標記 `OVERDRAWN`，不夾到 0。
5. **CPU 3D 幾何預覽**與 **LDraw 下載**。

---

## `n/a` 不是 `FAIL`

兩頁都沒有跑解碼器，所以「終止原因」不存在。讀取終止原因的兩項檢查
（`termination_accepted` 與 `deterministic_core_success`）如實記為 `null`，
在頁面上顯示 `n/a`，**不會寫成 `false`**。把「沒人知道」寫成「否」，
是往看起來謹慎的方向交出一個從未發生的失敗。

交付判定用的是**不含終止原因的九項靜態檢查**，這份清單直接讀自
`scripts/27_delivery.py` 的 `DELIVERY_CHECKS`，介面沒有自己的副本。

---

## 下載的條件

**只有真正通過靜態交付條件時，才會出現 3D 預覽與 LDraw 下載。**

沒通過就兩者都沒有：沒有預覽、沒有下載連結，也沒有可用的 handle。
沒有通過檢查的結構不會被畫成一張看起來合法的圖片。

預覽與 LDraw 出自**同一份磚清單**（`report["result"]["text"]`），
由既有的 `write_ldraw` 與 `write_preview` 在一個暫存目錄裡產生，
讀成位元組後暫存目錄即刪除。兩者只活在本次執行的記憶體裡：

- **不寫入專案的 `artifacts/`**，也不寫入其他任何專案路徑。
- 重新啟動之後舊的 handle 就失效，頁面會說明原因並請你重新送出。
- 記憶體中最多保留最近 8 份，超過的自動丟棄。

---

## 失敗時會看到什麼

| 情況 | 結果 |
|---|---|
| 庫存空白、非法零件、兩種旋轉拼法同時給出 | 回到第一頁，指名原因，**保留已輸入的內容** |
| 不適用的欄位（compare + CP-SAT 控制） | 同上，並指名該欄位 |
| 缺目錄檔 | 第一頁事先標示；送出後受控拒絕並說明缺哪個檔 |
| 目錄不是 train-only | 受控拒絕（檔名必須以 `_train.jsonl` 結尾，每列必須 `split=train`） |
| 沒有可組候選 | 第二頁，狀態與逐件缺件證據，沒有預覽或下載 |
| CP-SAT 無解 | 第二頁，`INFEASIBLE` |
| CP-SAT 逾時 | 第二頁，`UNKNOWN` 並標記為逾時 |
| 有鋪排但沒通過復驗 | 第二頁，`tiling_found_but_not_delivery_ready` 與失敗理由 |
| 跨來源送出（外部 `Origin`） | 403，指明來源不符，且**不會執行任何交付流程** |
| 缺少／不符／重複的表單金鑰 | 403，指明缺哪一項 |
| 非半形數字、位數過長、數量超出世界容量 | 400，指名該欄位與該規則 |
| 介面本身出錯 | 一句話的錯誤頁；診斷寫到終端機，**頁面上不顯示 traceback** |

---

## 邊界

**這個介面不做的事：**

- **不生成、不載入權重、不使用 GPU。** 沒有 `--generate`，沒有解碼器入口。
- **不啟用 Phase 3 placement gate。** 該閘門從未正式評估。
- **不執行正式評估、不讀取任何已封存的評估案例、不產生 Success@K。**
- **不複製判定邏輯。** 每一次送出都經由 `scripts/27_delivery.py` 自己的
  `make_payload`，也就是命令列走的那一條；頁面與命令列因此不可能對
  「什麼算可交付」有不同意見。
- **不寫入正式產物。** 預覽與下載在記憶體與暫存目錄，不進 `artifacts/`。
- **不對外服務。** 只綁 loopback，另外檢查 `Host` 標頭是否為 loopback
  （避免任何解析到 127.0.0.1 的名稱被瀏覽器拿來驅動這個伺服器）。
  沒有任何旗標可以放寬——發布、部署、傳輸都不在它的用途之內。

### 跨站送出（CSRF）與同源

綁 loopback 擋掉的是**網路**，不是**瀏覽器**。使用者開著的任何一個網頁都可以
往 `http://127.0.0.1:8765/result` 送一份表單，瀏覽器會照送。因此每一次會改變
狀態的送出都必須同時滿足兩個條件，缺一律受控拒絕（HTTP 403，可讀訊息、
不顯示 traceback、不執行任何交付流程）：

1. **來源篩選**。若 `Origin` 是結構化網址，它的 scheme、host 與 **port** 都必須
   是這個伺服器自己。host 允許任何 loopback 拼法（`127.0.0.1`／`localhost`／
   `[::1]` 都是同一台），但**同機的另一個 port 是另一個應用程式，算外部**。
   `null` 是不透明來源，既不當成同源、也不直接授權；它會繼續接受下一項表單
   金鑰驗證。這是因為實際 Chrome 的頂層本機分頁也曾送出 `Origin: null`，若在
   讀取金鑰前一律擋下，會把真正的操作者鎖在結果頁外。
2. **表單金鑰**。第一頁的表單帶一個隱藏欄位 `csrf_token`，值是**每個行程各自
   產生**的一把不可猜測的金鑰。跨來源的頁面可以「造成」送出，但它讀不到
   第一頁的 HTML，因此拿不到這把金鑰。缺金鑰、金鑰不符、或金鑰被送出兩次，
   都拒絕。比對用 `hmac.compare_digest`。

金鑰隨行程產生：伺服器重新啟動後，停留在舊分頁裡的金鑰就失效。拒絕發生在
**任何交付工作之前**，不是先跑完再說。

`Origin: null` 本身不會放行任何工作：缺少、錯誤或重複的 `csrf_token` 仍在任何
交付工作之前回應 403。真正帶有外部網址的 Origin，即使偷塞一個金鑰欄位，也會在
讀取本體之前拒絕。頁面另以 CSP `frame-ancestors 'none'` 禁止被其他網站嵌入，
避免用 iframe 誘導操作者點擊本機介面。

### keep-alive 連線不會被污染

在 keep-alive 連線上回應一個「本體還沒被讀走」的請求，那些位元組會留在 socket
裡，下一個請求就會從本體中間開始解析——攻擊者可以把一整個 HTTP 請求塞在被拒絕
的本體裡，讓伺服器把它當成第二個請求執行。因此凡是略過本體的拒絕
（`Host` 不符、跨來源、`Content-Type` 不符、`Content-Length` 缺失或過大、
分塊傳輸）都會送出 `Connection: close` 並關閉連線。這個判斷寫在所有回應共用的
同一個出口，而不是每個拒絕點各寫一次。正常送出（本體已讀）不受影響，
連線照常沿用。

**關於 `torch` 的誠實說明。** 交付報告必須由既有的 scorer 計算，而
`src.eval.scoring` 匯入 `src.generation.brickgpt`，後者在模組層級就
`import torch` 與 `transformers`。因此執行這個介面時，`torch` 會**間接**
出現在 `sys.modules` 裡——這是既有 scorer 鏈的性質，**命令列
`scripts/27_delivery.py` 早就如此**，不是介面帶進來的。可以誠實斷言、也已經
用測試釘住的是：

1. `src/ui/` 與 `scripts/29_ui.py` **沒有**匯入任何模型模組，也沒有呼叫任何
   權重載入或解碼入口；
2. 把 `BrickGPT`、`load_tokenizer`、`load_finetuned`、`from_pretrained` 與
   `generate` 全部換成會拋例外的替身之後，整條成功路徑仍然跑得完；
3. 這個介面**沒有**引入任何命令列尚未引入的重量級模組。

要讓 `torch` 完全不被匯入，必須把 `src/generation/brickgpt.py` 的匯入改成延遲
匯入。那是 scorer 綁定來源鏈上的檔案，本輪未獲授權更動，因此沒有改。

**連通性不是支撐，也不是穩定性。** `stud_only_connected` 是相鄰層 footprint
的 2D 交集；接觸地面同樣不是穩定性結果。這個專案沒有任何東西檢查質心、力矩
或作品站不站得住。**不是物理支撐**這件事寫在兩頁的頁尾，不是只寫在這裡。

**這裡沒有指標。** 頁面上任何數字都不是指標，不可與 Phase 2 比較，也不可改名
成 `Structural`／`Semantic`／`Full Success@K`。求解秒數是這台機器這一次的實測，
不是模型或求解器的本質速度。

---

## 測試

```bash
HF_HUB_OFFLINE=1 PYTHONDONTWRITEBYTECODE=1 \
  ./.venv/bin/python -m pytest tests/test_ui.py -q -p no:cacheprovider
```

`tests/test_ui.py` 不需要任何人工點擊：表單、執行與渲染都是純函數，
HTTP 介面則以真實 socket 綁在 `127.0.0.1:0` 上，用 `http.client` 驅動。
涵蓋頁面切換、欄位適用性、成功與各種失敗路徑、下載條件、輸出一致性、
跳脫（XSS）、loopback 邊界，以及「不得載入模型」。

集中修正的四項各自有能讓舊版變紅的整合測試：跨來源與金鑰以真實 socket 送出；
連線污染用原始位元組把一個完整的 HTTP 請求塞進被拒絕的本體，再數回應裡出現
幾個 `HTTP/1.` 狀態行（修好是 1，沒修是 2）；數值入口逐條以 HTTP 檢查是 400
而不是 500；`PROJECT_STATUS.md` 那一項則實際複製一棵沒有該檔的樹，跑那一個
節點，確認它 **skip 而不是 FileNotFoundError**。

**這份測試檔會被公開，`PROJECT_STATUS.md` 不會。** 讀取它的那一個測試在缺檔時
以 `artifact-only:` skip，節點 id 也登記在私有的 release gate 裡；公開樹因此
不會因為邊界正常運作而變紅。

---

## 這個介面不能證明什麼

- 它不證明檢索有用。詞彙 baseline 沒有經過任何語意檢索評估。
- 它不證明 F-pipeline 有效。F-pipeline 已實作但**尚未正式評估**。
- 它不證明碰撞／連通拒絕層改善了任何成功率。該層從未有過正式指標。
- 它不改變 `PROJECT_STATUS.md` 裡任何已封存的數字，一個都沒有。

模型研究線維持結束。這一輪新增的是介面，不是研究結果。

---

*本專題與任何積木製造商無關，介面不使用任何第三方商標、標誌或角色，
輸出格式的 LDraw 零件庫本身也不隨附於此。*
